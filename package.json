{
  "name": "pos-system",
  "version": "1.0.0",
  "description": "POS Supermarket System",
  "scripts": {
    "install:all": "cd backend && npm install --include=dev && cd ../frontend && npm install --include=dev",
    "build:frontend": "cd frontend && npm run build",
    "build:backend": "cd backend && npx tsc",
    "prisma:setup": "cd backend && npx prisma generate && npx prisma migrate deploy",
    "build": "npm run install:all && npm run build:frontend && npm run build:backend && npm run prisma:setup",
    "start": "cd backend && node dist/src/index.js"
  },
  "engines": {
    "node": ">=18"
  }
}
