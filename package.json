{
  "name": "pos-system",
  "version": "1.0.0",
  "description": "POS Supermarket System",
  "scripts": {
    "dev:backend": "cd backend && npm run start",
    "dev:frontend": "cd frontend && npm run dev",
    "prisma:push": "cd backend && npx prisma db push",
    "prisma:seed": "cd backend && npx prisma db seed",
    "prisma:setup": "cd backend && npx prisma generate && npx prisma db push && npx prisma db seed"
  },
  "engines": {
    "node": ">=18"
  }
}
